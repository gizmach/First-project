Migrate = require('loopback-component-auto-migrate/lib/auto-migrate');

/**
 * Создание таблиц в базе по описанным моделям с параметром "public": true
 * @param {*} app 
 */
//module.exports = function(app) {
//    Migrate(app,app.models).then(console.log(1));
//}